# Mistakery — Сводный статус (индекс всего пути)

Дата обновления: 15 июля 2026. Ветка: `quality-prototype`. Это единая точка: где мы, что решено, что дальше. Детали — по ссылкам.

## Мастер-план и решения

- Мастер-план (6 фаз): [`2026-07-15-quality-prototype-plan.md`](2026-07-15-quality-prototype-plan.md).
- **Залоченные решения** (память проекта `design-direction-frame-shuffle-conditional-arcs`):
  - Модель реиграбельности = **Вариант B** (условный флаг-гейтинг в стиле Reigns, не жёсткие `next`-цепочки).
  - **«Рамка — достаточно»**: арки причинно-цельные, различие прогонов из обрамления; параллельные биты для v1 не вводим.
  - Начинаем с **Agents** как эталонного среза; бет-лист Agents = **8 битов** (`AGENT_03B_WILD` = повышение `PRESS_CAPITALISM`).
  - `resourceRange` сохраняем и делаем главным мотором вариации.

## Статус по фазам

| Фаза | Что | Статус |
|---|---|---|
| 0 | База: ветка + зелёный baseline (107) | ✅ done (`718d0cc`) |
| 1 | Аудит Agents-среза (структура+голоса) | ✅ done → [audit](2026-07-15-phase1-agents-audit.md) |
| 2 | Движок под Reigns (флаг-гейтинг) | 🔄 **тут сейчас** → [plan](2026-07-15-phase2-engine-plan.md) |
| 3 | Форма колоды и ambient-набор (апрув автора) | ⬜ pending (решение «рамка достаточно» принято; число карт — потом) |
| 4 | Ремонт арок (чистый causal spine, +бит 3B, раскрыть обрезанное) | ⬜ pending |
| 5 | Голоса (Fable): переписать реплики | ⬜ pending (список «СОХРАНИТЬ» заморожен) |
| 6 | Баланс и проверка (сид-симуляции, 3 живых прохода, пороги рубрики) | ⬜ pending |

## Фаза 2 — детальный статус (где именно мы углубились)

Порядок принят по ревью Codex: [review-1](2026-07-15-phase2-codex-review-request.md), [review-2](2026-07-15-phase2-codex-review-2.md).

- **2.1 — Флаг-гейтинг (pool-режим)** ✅. Свободные биты `AGENT_01/02/03` тасуются из пула; склейка `AGENT_04→07` forced. 10 инвариант-тестов зелёные.
- **2.2 — генерик pool-континуация** ✅ (Codex round-2 P1: кризис+пул, resume-в-пул, F3-пересбор).
- **2c — повысить `PRESS_CAPITALISM` → `AGENT_03B_WILD`** ✅ структурно (`fb02f84`): бит добавлен (арка 8 битов), `AGENT_04` перегейчен на `hype_consequence_seen`, legacy pressure-delay убран (артефакты стека исчезли). Копия — плейсхолдер (Fable в Фазе 5). Флаги `agents_positioned_*` читаются битом 5 в Фазе 5 (сейчас числятся «dead flag»).
- **2.2-миграция** ⬜ ← следующий шаг: payroll/dev/b3 на `delay`, типизировать callbacks (`callbackOnly`+`requires`), закрыть F4/answer-3 (force-delivery guard).
- **2.2-снос машинерии** ⬜: убрать boundaries/reservations/slots/locks + удалить мёртвую карту PRESS_CAPITALISM.
- **2.4 — переписать legacy-тесты + статистические инварианты** ⬜: ~19 красных трейсов под новую 8-битную модель; 10k прогонов (тасовка, spacing, достижимость, нет тупиков) → 100% зелёное.

Текущее состояние тестов: **19 красных**, все legacy-structure (старая машинерия + Agents-трейсы под 7 битов + retired PRESS_CAPITALISM + `agents_positioned_*` без читателя до Фазы 5). Движковых регрессий нет. Новые инварианты 11/11.

## Хронология коммитов

`718d0cc` baseline → `ab1e190` pool-движок → `887da90` учёт ревью-1 → `198215f` схема ревью-2 → `a82a232` док ревью-2 → `5545a10` фиксы continuation (round-2 P1).

## Как продолжить в новой сессии

Прочитать этот файл → память проекта → `phase2-engine-plan.md`. Задача «Фаза 2» и «Фаза 2.2» в трекере. Следующий шаг — 2c. Следующий Codex-чекпоинт — после 2c + миграции.
